import styled, { createGlobalStyle } from 'styled-components';


const Root = styled.createGlobalStyle`
:root {
    --primary: #000;
    --primaryAccent: #fff;
    --accent: #f04;
}
`;

export default Root;