import React from "react";
import { AboutSection, AboutContainer } from '../styled-components/routes/hero/index';

const Hero = () => {
    return (
        <AboutSection>
            <AboutContainer>
                About
            </AboutContainer>
        </AboutSection>
    );
};

export default Hero;
