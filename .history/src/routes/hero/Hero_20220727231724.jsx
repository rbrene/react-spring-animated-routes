import React from "react";
import { HeroSection, HeroContainer } from '../../styled-components/routes/hero/index';


const Hero = () => {
    return (
        <HeroSection>
            <HeroContainer>
                Hero
            </HeroContainer>
        </HeroSection>
    );
};

export default Hero;
